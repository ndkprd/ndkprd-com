<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>@ndkprd</title>
  <link rel="stylesheet" href="style.css">
  <link rel="icon" href="{{ web_favicon_url }}">
</head>
<body>
  <div class="container">
    <div class="card">
      <div class="profile-section">
        <img src="{{ web_profile_image_url }}" alt="Profile" class="profile-img">
      </div>
      <div class="info-section">
        <h1>{{ web_headline }}</h1>
        {% for description in web_descriptions %}
          <p>{{ description }}</p>
        {% endfor %}
        <div class="links">
          {% for link in web_links %}
            <a href="{{ link.url }}">{{ link.name }}</a>{% if not loop.last %} |{% endif %}
          {% endfor %}
        </div>
      </div>
    </div>
  </div>
  <footer>
    {{ web_footer }}
  </footer>
</body>
</html>
